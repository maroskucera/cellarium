# Cellarium root-level environment defaults.
# Committed to git — do NOT put secrets here.
# Use .env.local for local overrides (gitignored).

DATABASE_URL=postgres://localhost:5432/cellarium?sslmode=disable
